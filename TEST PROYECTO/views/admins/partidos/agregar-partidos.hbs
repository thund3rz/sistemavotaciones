<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- linkeando css y boostrap css-->
    <link rel="stylesheet" type="text/css" href="/assets/css/bootstrap/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/assets/css/style.css">
    <!----------------------->
    <title>Document</title>
</head>
<body>
    {{#if EstaAutenticado}}

    <div class="card">
    
    <div class="pricing-header p-3 pb-md-4 mx-auto text-center">
      <h1 class="display-4 fw-normal">{{#if ModoEditar}}Edicion de los partidos.{{else}}Agregar partidos.{{/if}}</h1>
    </div>

        <!--cuerpo del formulario-->
        <div class="card-body">

            <form method="POST" enctype="multipart/form-data" action="{{#if ModoEditar}}/edicion-partidos{{else}}/agregar-partidos{{/if}}">
            <!--campo nombre del partido-->
            <div class="mb-3">
                <label for="nombrep" class="form-label">Nombre del partido:</label>
                <input type="text" class="form-control" name="Nombrep" id="nombrep" value="{{#if ModoEditar}}{{partido.nombre}}{{/if}}"/>
            </div>
            <!-------------------->
            <!--campo descripcion del partido-->
            <div class="mb-3">
                <label for="descripcionp" class="form-label">Descripcion del partido:</label>
                <input type="text" class="form-control" name="Descripcionp" id="descripcionp" value="{{#if ModoEditar}}{{partido.descripcion}}{{/if}}"/>
            </div>
            <!-------------------->
            <!--campo logo del partido-->
            <div class="mb-3">
                <label for="formFileSm" class="form-label">Seleccione una imagen</label>
                <input class="form-control form-control-sm" id="formFileSm" name="ImagePath" type="file">
            </div>
            <!-------------------->
            <!--campo estado del partido-->
            <div class="mb-3">
                <label for="estadop" class="form-label">Estado:</label>
                
                <select id="estadop" name="Estadop" class="form-select">
                    <option value="{{#if ModoEditar}} {{partido.estado}} {{else}} {{/if}}"selected>{{#if ModoEditar}} {{partido.estado}} {{else}}Seleccione el estado del partido{{/if}}</option>
                    <option value="true">Activo</option>
                    <option value="false">Inactivo</option>
                </select>
            </div>
            <!-------------------->
            <!--campo input hidden-->
            {{#if ModoEditar}} 
            
                <input type="hidden" name="partidoId" value="{{partido.id}}">
            
            {{/if}}
            
            <!-------------------->

            <!--boton guardar/actualizar-->
            <button type="submit"  id="btn-guardar" class="btn btn-primary float-end">{{#if ModoEditar}}Actualizar{{else}}Guardar{{/if}}</button>
            <!-------------------->
        </form>
        </div>  
        <!-------------------->

    </div>

<a href="/crud-partidos" class="btn btn-secondary d-block w-100">Volver</a>

    {{else}}
        <div class="pricing-header p-3 pb-md-4 mx-auto text-center">
            <h1 class="display-4 fw-normal">No autorizado para ver esta seccion, debe estar logeado como administrador.</h1>
        </div>
{{/if}}
</body>

<!--linkeando js, boostrap js y jquery-->
<script src="/assets/js/jquery/jquery-3.6.0.min.js"></script>
<script src="/assets/js/bootstrap/bootstrap.min.js"></script>
<!----------------------->
</html>